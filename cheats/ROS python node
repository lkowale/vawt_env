cd package_name
mkdir scripts
cd scripts
create python file
chmod +x rotor_state.py
Add the following to your pkg_dir/CMakeLists.txt

catkin_install_python(PROGRAMS scripts/rotor_state.py
  DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
)

$ cd ~/vawt_env
$ catkin_make